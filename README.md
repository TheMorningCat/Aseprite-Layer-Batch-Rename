# Aseprite Layer Batch Rename

Aseprite 图层批量重命名工具

自动生成图层名称表达式、保留差异内容和图层序号生成

<img width="1529" height="755" alt="图片" src="https://github.com/user-attachments/assets/2a2574cc-470d-43db-a037-b8e1b48983bb" />

***

## 安装

1. 下载 `LayerBatchRename.lua`
2. 在 Aseprite 中执行 `File > Scripts > Open Scripts Folder`
3. 将 `LayerBatchRename.lua` 复制到打开的文件夹中
4. 在 Aseprite 中执行 `File > Scripts > Rescan Scripts Folder`

之后可通过 `File > Scripts > LayerBatchRename` 运行

***

## 表达式语法

以下内容会根据图层名称自动生成，只需对`新表达式`进行修改即可，点击重置可恢复默认表达式。

### 变量占位符 #1 #2 ...

对比图层名中存在差异的内容进行占位，以保留图层命名中的差异部分

`#1` `#2` ... 是自动检测生成的变量占位符，用于标记图层名中的变化部分。

在新表达式中，`#1`\~`#9` 引用原始表达式中捕获的文本，保留原始变化内容。

> **注意**：`#d` 为序号保留，不作为变量名使用。变量最多支持 61 个，超过时脚本会提示错误。

### 序号占位符 #d

在新表达式中插入 `#d` 即可生成序号。

"位数"控制序号补零：位数 = 3 即补零至 3 位（如 001）。

### 序号规则

序号从最下方选中图层开始，由"序号起始"值起，向上每层增加"序号步长"。勾选"倒序"后序号从最上方图层开始递增。

### 使用示例

| 原始名称                        | 自动生成表达式  | 新表达式     | 起始 | 步长 | 新名称                      |
| --------------------------- | -------- | -------- | -- | -- | ------------------------ |
| 闪光,闪光 1, 闪光 1 拷贝, 闪光 1 拷贝 2 | 闪光#1     | 闪光-#d    | 1  | 1  | 闪光-1,闪光-2, 闪光-3, 闪光-4    |
| 角色-笑-眼睛, 角色-笑-嘴, 角色-哭-眼睛    | 角色-#1-#2 | 角色-#2-#1 | 1  | 1  | 角色-眼睛-笑, 角色-嘴-笑, 角色-眼睛-哭 |

> **提示**：复制图层时，`#1` 可匹配空串（如"闪光 1"中 #1 匹配空）

| 原始名称                  | 自动生成表达式 | 新表达式    | 起始 | 步长 | 位数 | 新名称                   |
| --------------------- | ------- | ------- | -- | -- | -- | --------------------- |
| 图层1002, 图层1005,图层1007 | 图层100#1 | 图层100#d | 1  | 1  | 0  | 图层1001, 图层1002,图层1003 |
| 图层102, 图层202          | 图层#102  | 图层#d    | 1  | 1  | 3  | 图层001, 图层002          |

> **提示**：`#1` 只占一个字符位置，后面的数字为字面量。表达式"图层#12"中 #1 是变量，"2"是固定文字。新表达式"图层#13"表示变量 #1 后接固定文字"3"。

### 图层名包含 # 字符

若图层名的共同部分本身包含 `#1`、`#2` 等文本，软件会自动跳过这些编号，将占位符分配到其他编号。

例如图层名 `item#1_shadow`、`item#1_light`，自动生成的表达式为 `item##1_#2`，其中 `##1` 是字面量 `#1`，`#2` 是变量占位符。

在新表达式中，若引用了原始表达式中不存在的占位符（如原始表达式中没有 `#1` 却在新表达式中写了 `#1`），该占位符不会生成任何文本。

***

## 其他功能

| 区域     | 说明                       |
| ------ | ------------------------ |
| 排除图层组  | 开启后不对图层组进行重命名            |
| 排除图层   | 开启后不对普通图层进行重命名           |
| 获取子对象  | 开启后将选中图层组的所有子对象（含嵌套）加入列表 |
| GitHub | 复制项目 GitHub 链接到剪贴板       |

## 注意事项

1. **图层名中的 #**：若图层名需要包含 `#` 字符，在新表达式中使用 `##` 转义
2. Aseprite 允许不同图层使用相同名称。当新表达式生成的名称存在重复时会弹窗**提示**存在同名图层，列出重复的名称，如需避免同名，可在提示后撤销（Ctrl+Z）并修改表达式

